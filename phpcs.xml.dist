<?xml version="1.0"?>
<ruleset name="Extra Chill Contact">
	<description>PHPCS configuration for the Extra Chill Contact plugin (WordPress-Extra based; the tools resolve from the Homeboy wordpress extension).</description>

	<!-- Scan the plugin root by default -->
	<file>.</file>

	<!-- WordPress-Extra includes WordPress-Core + WordPress-Docs -->
	<rule ref="WordPress-Extra">
		<!-- Exclude naming convention rules to support PSR-4 codebases -->
		<exclude name="WordPress.Files.FileName.NotHyphenatedLowercase"/>
		<exclude name="WordPress.Files.FileName.InvalidClassFileName"/>
		<exclude name="WordPress.NamingConventions.ValidFunctionName.MethodNameInvalid"/>
		<exclude name="WordPress.NamingConventions.ValidVariableName.VariableNotSnakeCase"/>
		<exclude name="WordPress.NamingConventions.ValidVariableName.UsedPropertyNotSnakeCase"/>
		<exclude name="WordPress.NamingConventions.ValidVariableName.PropertyNotSnakeCase"/>
		<exclude name="WordPress.NamingConventions.ValidVariableName.InterpolatedVariableNotSnakeCase"/>

		<!-- Exclude PEAR/Squiz function spacing rules that conflict with WordPress formatting -->
		<!-- These rules fight with WordPress spacing rules, causing PHPCBF infinite loops -->
		<exclude name="PEAR.Functions.FunctionCallSignature"/>
		<exclude name="Squiz.Functions.FunctionDeclarationArgumentSpacing"/>
		<exclude name="Squiz.Functions.MultiLineFunctionDeclaration"/>
	</rule>

	<!-- Exclude common non-source directories. tests/ defines WordPress shims
	     (a WP_Error stub plus function declarations) that intentionally trip
	     OO/function-separation and alternative-function sniffs. -->
	<exclude-pattern>*/vendor/*</exclude-pattern>
	<exclude-pattern>*/node_modules/*</exclude-pattern>
	<exclude-pattern>*/dist/*</exclude-pattern>
	<exclude-pattern>*/build/*</exclude-pattern>
	<exclude-pattern>*/tools/*</exclude-pattern>
	<exclude-pattern>*/tests/*</exclude-pattern>

	<!-- Show sniff codes in reports for easier targeting -->
	<arg value="s"/>

	<!-- Use colors in output -->
	<arg name="colors"/>

	<!-- Check PHP files only -->
	<arg name="extensions" value="php"/>
</ruleset>
